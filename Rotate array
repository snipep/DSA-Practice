def rotateArray(arr, k):
    n = len(arr)
    k = k % n  
    return arr[k:] + arr[:k]




n = int(input())
arr = [int(i) for i in input().split()]
k = int(input())
rotated_arr = rotateArray(arr, k)

print(*rotated_arr) 
