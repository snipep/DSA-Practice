class Solution(object):
    def maxSubArray(self, nums):
        sum=0
        max1=nums[0]
        for i in nums:
            i<0
        
        for i in range(len(nums)):
            sum += nums[i]
            max1=max(sum,max1)
            sum=max(sum,0)
        print(max1)
        return max1
